# Roof Detection Service Dependencies
# �ݶ�����������

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# AI/ML
# Pin ultralytics to the version verified with preroof weights on your machine
ultralytics==8.3.170
# Keep torch/torchvision broad but compatible; adjust if future regressions
torch>=2.0.1,<3.0.0
torchvision>=0.15.2,<1.0.0

# Image processing
opencv-python-headless==4.8.1.78
Pillow==10.0.0
numpy==1.24.3

# Test client dependency (FastAPI uses httpx)
httpx==0.27.2

# Geometry
shapely==2.0.1
